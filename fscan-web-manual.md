# fscan-web 使用手册

> **版本**: 2.2.0 | **文件**: `fscan-web_2.2.0_windows_x64.exe` (10.3 MB)
>
> fscan 的 Web 可视化管理界面版本。底层扫描能力与命令行版完全一致，但通过浏览器操作，无需记忆命令行参数。
>
> **重要声明**: 本工具仅面向**合法授权**的企业安全建设行为。使用前请确保已获得授权，符合当地法律法规，**不对非授权目标扫描**。作者不承担任何非法使用产生的后果。

---

## 目录

- [简介](#简介)
- [与命令行版的关系](#与命令行版的关系)
- [参数说明](#参数说明)
- [启动方式](#启动方式)
- [界面功能概览](#界面功能概览)
- [典型工作流](#典型工作流)
- [使用场景](#使用场景)
- [常见问题](#常见问题)

---

## 简介

fscan-web 是 fscan 的条件编译版本（编译时添加 `-tags web`），在命令行扫描引擎之上内置了一个 Web 服务器，提供：

- **图形化任务管理**：通过表单配置扫描参数，无需记忆命令行
- **实时结果推送**：WebSocket 实时推送扫描进度和结果
- **扫描预设保存**：保存常用扫描配置，一键复用
- **资产项目管理**：管理多个扫描项目和目标资产
- **多语言支持**：中文 / English 界面切换
- **深色模式**：支持暗色主题

**适用人群**：

- 不熟悉命令行的安全新人
- 需要同时管理多个扫描任务的安全团队
- 希望直观查看结果的场景
- 需要给非技术人员展示扫描结果的场景

---

## 与命令行版的关系

```
fscan (命令行版) ──编译时加 -tags web──→ fscan-web (Web 版)
```

| 对比维度 | fscan（命令行版） | fscan-web（Web 版） |
|----------|-------------------|---------------------|
| 启动方式 | 每次执行一条命令 | 启动 Web 服务，浏览器操作 |
| 参数输入 | 命令行参数 | Web 表单 |
| 结果查看 | 文本文件 / 终端输出 | Web 界面结构化展示 |
| 多任务管理 | 需要多个终端窗口 | 统一任务面板管理 |
| 适合场景 | 脚本自动化、单次快速扫描 | 多人协作、长期项目管理 |
| 扫描能力 | 完全一致 | 完全一致 |

---

## 参数说明

fscan-web 只有两个参数：

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `-port` | int | 10240 | Web 服务器监听端口 |
| `-lang` | string | `zh` | 界面语言: `zh`(中文) / `en`(英文) |

---

## 启动方式

```powershell
# 使用默认端口 10240 启动（最简方式）
.\fscan-web_2.2.0_windows_x64.exe

# 指定端口启动（当默认端口被占用时）
.\fscan-web_2.2.0_windows_x64.exe -port 8080

# 英文界面
.\fscan-web_2.2.0_windows_x64.exe -port 8080 -lang en
```

启动后，在浏览器中打开 `http://127.0.0.1:10240`（或你指定的端口）。

> **安全提示**：fscan-web 默认仅监听本地回环地址（127.0.0.1），外部无法直接访问。如需远程访问，请使用 SSH 隧道或 VPN 等安全方式，不要在公网直接暴露。

---

## 界面功能概览

| 功能区域 | 说明 |
|----------|------|
| **任务面板** | 查看所有扫描任务的状态、进度、结果 |
| **新建任务** | 通过表单配置扫描目标、端口、模块、凭据等参数 |
| **任务监控** | 实时查看正在运行的任务进度（WebSocket 推送） |
| **结果展示** | 结构化显示扫描结果：主机、端口、服务、漏洞 |
| **预设管理** | 保存常用的扫描配置，下次一键加载 |
| **资产管理** | 管理多个项目/客户的目标资产 |
| **系统设置** | 语言切换、主题切换（深色/浅色） |

---

## 典型工作流

### 第一次使用

```powershell
# 1. 启动 Web 服务
.\fscan-web_2.2.0_windows_x64.exe -port 8080
```

```
# 2. 浏览器打开
http://127.0.0.1:8080

# 3. 点击 "新建任务"

# 4. 填写扫描参数：
#    - 目标：192.168.1.0/24
#    - 端口：使用默认（约140个常用端口）
#    - 模块：全选（默认）

# 5. 点击 "开始扫描"

# 6. 在任务监控页面查看实时进度

# 7. 扫描完成后，点击任务查看结构化结果：
#    - 存活主机列表
#    - 开放端口分布
#    - 服务识别结果
#    - 漏洞详情

# 8. 导出报告（JSON / CSV）
```

### 日常使用

```
1. 打开 fscan-web → 加载之前保存的扫描预设
2. 修改目标 IP（如果是新的网段）
3. 一键启动扫描
4. 查看结果，对比上次扫描有无新增资产/漏洞
```

---

## 使用场景

### 场景1：团队协作的资产梳理

多个安全工程师需要协作扫描和追踪公司内网资产时，可以：

1. 在一台内网服务器上启动 fscan-web
2. 团队成员各自通过浏览器访问
3. 每个人创建自己的扫描任务，互不干扰
4. 所有结果统一存储在服务器上，方便汇总

### 场景2：定期安全巡检

需要定期（如每周/每月）对固定网段进行安全巡检时：

1. 创建扫描预设（保存目标网段、端口、模块配置）
2. 每次巡检只需加载预设，点击开始
3. 结果与历史记录对比，发现新增资产或新漏洞

### 场景3：非技术人员查看结果

需要给领导或客户展示扫描结果时：

1. 用 fscan-web 完成扫描
2. 直接在浏览器中展示结构化结果
3. 按风险等级、服务类型筛选结果
4. 无需在终端中翻找文本文件

### 场景4：学习与教学

安全新人学习内网扫描时：

1. 不需要记忆复杂的命令行参数
2. 通过表单直观理解每个参数的含义
3. 实时看到进度反馈，理解扫描流程
4. 结果结构化展示更易解读

---

## 常见问题

### Q: 启动时报 "address already in use"？

端口被占用。换一个端口即可：

```powershell
.\fscan-web_2.2.0_windows_x64.exe -port 8888
```

### Q: 扫描速度和命令行版一样吗？

完全一样。Web 界面只是前端展示层，底层扫描引擎是同一个。

### Q: 可以多人同时使用吗？

可以。多个浏览器可以同时打开 fscan-web，各自创建和管理任务。但需要注意扫描任务的网络负载，不建议同时跑大量扫描。

### Q: 关闭浏览器后扫描会中断吗？

不会。扫描任务在服务器端运行，关闭浏览器不影响任务执行。下次打开浏览器可继续查看进度和结果。

### Q: 扫描结果保存在哪里？

与命令行版一样，默认保存在启动目录下的 `result.txt`（或你指定的路径）。可在任务设置中修改输出路径和格式（txt/json/csv）。

### Q: 如何在外网访问 fscan-web？

**不推荐**直接暴露到公网。安全的做法是使用 SSH 隧道：

```powershell
# 在本地机器建立 SSH 隧道
ssh -L 10240:127.0.0.1:10240 user@remote-server

# 然后本地浏览器访问
http://127.0.0.1:10240
```

---

> **最后提醒**：本工具是专业安全评估工具，请在获得合法授权的前提下使用。禁止对未授权目标进行扫描，使用者需自行承担所有法律责任。
